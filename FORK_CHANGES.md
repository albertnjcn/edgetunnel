# 本 Fork 的改动说明

基于上游 `cmliu/edgetunnel`（版本 2026-09-04）的小幅增强，向后兼容，不设置新变量时行为与上游完全一致。

## 1. TCP 拨号超时可配置

上游 `forwardataTCP` 内写死单路拨号超时 1000ms，高延迟链路（跨洲、劣质中转）下合法连接可能被误杀。现改为可用环境变量 `CONNECT_TIMEOUT` 覆盖（毫秒，最小 500，默认仍为 1000）；超时后关闭该路连接并走反代（proxyIP）兜底的原逻辑不变。

## 2. 大流量下载断流——诊断结论（无需改代码）

Workers 日志中的两类错误都是**运行时（isolate）级崩溃**，不是脚本 bug：

- `internal error; reference = xxx`：Workers 运行时内部错误，isolate 异常死亡（多与内存/CPU 上限相关，reference ID 是给 Cloudflare 支持排查用的）。stateless 模式下一个 isolate 同时服务多个请求，isolate 死亡时**其中所有在途连接一起断**——日志里显示 `GET /` 只是被记到的倒霉请求，不一定是真正断的下载连接。
- `Network connection lost.`：底层传输被撕断。既可能是 isolate 死亡的连带结果，也可能是客户端先断（下载器超时重试、网络切换），单看一侧日志分不清先后。

"下载到一定流量就断"的机理：**单次调用的资源上限随流量累积耗尽**——内存上限 128MB（免费/付费版相同，运行时在客户端收包慢于远端发包时内部缓冲增长），CPU 上限免费版仅 10ms/次调用、付费版默认 30s。吞吐越高、越接近上限，表现为"过了某个流量就断"。

并发的影响：不是"超并发数"这种硬错误，而是放大器——客户端开 mux 时多路下载共享一次调用（限额共享，死得更快）；多连接可能共享同一 isolate（压力叠加，死一片）。

**对策（免费版）：上限是按"每次调用"计算的，所以把大文件下载拆成多条并行短连接，每条连接各吃各的额度——等于把天花板乘以连接数：**

| 措施 | 说明 |
|---|---|
| 分段 + 断点续传下载器（核心） | aria2：`aria2c -x4 -s8 -k1M --min-split-size=1M -c --max-tries=0 --retry-wait=2 <URL>`；每段是独立隧道连接（独立调用），断了自动重连补段，断流无感 |
| 客户端关 mux | mux 会让多段挤进同一次调用共享额度，与上一条正好相反 |
| 别用浏览器单连接直下 | 浏览器下载是一条连接从头跑到尾（一次调用扛全部流量），最容易撞墙 |
| 并发取 4~8 | 每条连接都能拿到完整的单次调用额度；免费版 10 万请求/天也完全够用（每个分段只是一次请求） |

（付费版才有的选项：wrangler.toml `[limits] cpu_ms = 300000` 把 CPU 上限从 30s 提到 5 分钟，免费版固定 10ms 无此配置。）

注：已复核脚本下行链路逐块 await 发送、合包/队列均有上限（32KB grain / 16MB 队列），JS 侧内存卫生良好，无可修的泄漏点；剩余缓冲在运行时内部，代码层无法进一步收紧。

## 新增环境变量一览

| 变量 | 默认 | 说明 |
|---|---|---|
| `CONNECT_TIMEOUT` | `1000` | 单路 TCP 拨号超时（毫秒），≥500 生效 |

其余上游变量（`UUID`、`PROXYIP`、`PATH`、`KV` 绑定名等）均不受影响。
